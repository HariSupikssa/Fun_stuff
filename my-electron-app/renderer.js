// const information = document.getElementById('info')
// information.innerText = `Chrome(v${versions.chrome()}), Node.js (v${versions.node()}) and Electron (v${versions.electron}) `

// // since preload.js is commented out in main.js, and the variables come from preload.js, this is of no use anymore


